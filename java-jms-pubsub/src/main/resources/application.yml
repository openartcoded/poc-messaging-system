application:
  topic:
    subscription: ${JMS_TOPIC_SUBSCRIPTION:shared_topic}
    publish: ${JMS_TOPIC_PUBLISHING:mqtt_topic}

spring:
  artemis:
    broker-url: ${BROKER_URL:tcp://localhost:55516}
    user: ${JMS_USERNAME:root}
    password: ${JMS_PASSWORD:root}

server:
  port: 0
